<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="LightFont">
        <item name="customFont">Roboto-Light.ttf</item>
    </style>
    
    <style name="CondensedFont">
        <item name="customFont">RobotoCondensed-Regular.ttf</item>
    </style>
    
    <style name="Header" parent="CondensedFont">
        <item name="android:layout_marginLeft">@dimen/normal_padding</item>
        <item name="android:layout_marginTop">@dimen/medium_padding</item>
        <item name="android:layout_marginBottom">@dimen/small_padding</item>
        <item name="android:textColor">@color/Three</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="SettingsCell" parent="LightFont">
        <item name="android:paddingBottom">@dimen/normal_padding</item>
        <item name="android:paddingTop">@dimen/normal_padding</item>        
        <item name="android:layout_marginLeft">@dimen/normal_padding</item>
    </style>
    
    
    
</resources>
